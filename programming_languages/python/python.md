# Python

## PEP8

Naming convention.

- `CamelCase` for classes;
- `snake_case` for functions and methods;
- `snake_case` for modules with short names and with _ if it helps readibility;
- `lowercase` for packages the underscore are discouraged;
- `_lowercase` for native modules;
